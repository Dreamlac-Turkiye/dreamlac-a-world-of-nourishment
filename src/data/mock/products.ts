/**
 * @deprecated Preview compatibility alias. Production catalog content lives in
 * @/data/catalog/products and is composed with database settings by the
 * catalog service. Keep this re-export only for legacy admin previews.
 */
export { catalogProducts as mockProducts } from "@/data/catalog/products";
